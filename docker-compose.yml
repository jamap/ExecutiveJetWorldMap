version: '3.8'

services:
  aviation-planner:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: executive-aviation-planner
    ports:
      - "8080:80"
    restart: unless-stopped
