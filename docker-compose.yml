version: '3.8'

services:
  aviation-planner:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: executive-aviation-planner
    ports:
      - "80:80"
    restart: unless-stopped
